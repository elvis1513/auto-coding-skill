Use `auto-coding-skill` for this repository.

Install Claude targets with:

```bash
autocoding init --ai claude
```

Claude project installs use `.claude/skills/auto-coding-skill`.
See `src/auto-coding-skill/data/templates/ENGINEERING.md` as the single source of truth for workflow gates.
