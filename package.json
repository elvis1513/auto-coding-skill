{
  "name": "@elvis1513/auto-coding-skill",
  "version": "2.1.2",
  "description": "CLI installer for a generic .agents engineering workflow with optional CI/Jenkins and target-environment verification.",
  "type": "module",
  "bin": {
    "autocoding": "cli/src/index.js"
  },
  "files": [
    "cli/src",
    "cli/tests",
    "cli/assets/skill",
    "cli/assets/agents",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node -c cli/src/index.js && node -c cli/src/sync-assets.js && node -c cli/tests/index.test.js && node cli/tests/index.test.js && python3 -c \"import ast, pathlib; files = ['cli/assets/skill/scripts/ap.py', 'cli/assets/skill/scripts/core.py', 'cli/assets/skill/scripts/http_checks.py']; [ast.parse(pathlib.Path(rel).read_text(encoding='utf-8'), filename=rel, feature_version=(3, 11)) for rel in files]; print('python311-asset-grammar-ok')\" && python3 -m py_compile cli/assets/skill/scripts/ap.py cli/assets/skill/scripts/core.py cli/assets/skill/scripts/http_checks.py && python3 -c \"import pathlib, tomllib; files = sorted(pathlib.Path('cli/assets/agents').glob('*.toml')); [tomllib.loads(path.read_text(encoding='utf-8')) for path in files]; print('asset-agent-toml-ok')\" && find cli -type d -name '__pycache__' -prune -exec rm -rf {} +",
    "test:src": "python3 -c \"import ast, pathlib; files = ['src/auto-coding-skill/scripts/ap.py', 'src/auto-coding-skill/scripts/core.py', 'src/auto-coding-skill/scripts/http_checks.py']; [ast.parse(pathlib.Path(rel).read_text(encoding='utf-8'), filename=rel, feature_version=(3, 11)) for rel in files]; print('python311-src-grammar-ok')\" && python3 -m py_compile src/auto-coding-skill/scripts/ap.py src/auto-coding-skill/scripts/core.py src/auto-coding-skill/scripts/http_checks.py && python3 -c \"import pathlib, tomllib; files = sorted(pathlib.Path('src/agents').glob('*.toml')); [tomllib.loads(path.read_text(encoding='utf-8')) for path in files]; print('src-agent-toml-ok')\" && find src -type d -name '__pycache__' -prune -exec rm -rf {} +",
    "check:assets": "node cli/src/sync-assets.js --check",
    "sync-assets": "node cli/src/sync-assets.js",
    "release:check": "npm run sync-assets && npm run check:assets && npm run test:src && npm run test && npm pack --dry-run"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "agents",
    "skill",
    "automation",
    "engineering",
    "workflow",
    "ci",
    "jenkins",
    "monorepo",
    "docker",
    "local-testing"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/elvis1513/auto-coding-skill.git"
  },
  "homepage": "https://github.com/elvis1513/auto-coding-skill#readme",
  "bugs": {
    "url": "https://github.com/elvis1513/auto-coding-skill/issues"
  }
}
